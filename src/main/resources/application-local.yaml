logging:
  level:
    dev.langchain4j: INFO
    org.springframework.web.access:  INFO
    org.springframework.security: INFO
    org.hibernate.SQL: INFO
    org.hibernate.type: INFO
    liquibase: INFO

spring:
  datasource:
    url: "jdbc:postgresql://localhost:5432/basic-auth-db-local?reWriteBatchedInserts=true"
    driver-class-name: org.postgresql.Driver
    username: "admin"
    password: "password"
  jpa:
    properties:
      hibernate:
        order_inserts: true
        order_updates: true
